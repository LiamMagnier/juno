name: JunoMac
options:
  createIntermediateGroups: true
  deploymentTarget:
    macOS: "15.0"
  developmentLanguage: en
  knownRegions:
    - en
    - fr
  minimumXcodeGenVersion: 2.46.0
configs:
  Debug: debug
  Stable: release
  Next: release
packages:
  JunoNativeKit:
    path: ../../Packages/JunoNativeKit
  JunoCode:
    path: ../../Packages/JunoCode
targets:
  JunoMac:
    type: application
    platform: macOS
    sources:
      - path: App
      - path: Resources
        excludes:
          - Info.plist
          - JunoMac.entitlements
        buildPhase: resources
    dependencies:
      - package: JunoNativeKit
        product: JunoAPI
      - package: JunoNativeKit
        product: JunoAuth
      - package: JunoNativeKit
        product: JunoChatKit
      - package: JunoNativeKit
        product: JunoCodeKit
      - package: JunoNativeKit
        product: JunoCore
      - package: JunoNativeKit
        product: JunoDesignSystem
      - package: JunoNativeKit
        product: JunoStorage
      - package: JunoNativeKit
        product: JunoSync
      # Development-only visual QA harness; its symbols are all #if DEBUG, so it
      # contributes nothing to Stable/Release builds.
      - package: JunoNativeKit
        product: JunoPreviewSupport
      - package: JunoCode
        product: JunoCodeUI
      - package: JunoCode
        product: JunoCodeRuntime
      - package: JunoCode
        product: JunoCodeBridge
    configFiles:
      Debug: ../../Config/JunoMac-Debug.xcconfig
      Stable: ../../Config/JunoMac-Stable.xcconfig
      Next: ../../Config/JunoMac-Next.xcconfig
    settings:
      base:
        CODE_SIGN_ENTITLEMENTS: Resources/JunoMac.entitlements
        ENABLE_USER_SCRIPT_SANDBOXING: NO
        GENERATE_INFOPLIST_FILE: NO
        INFOPLIST_FILE: Resources/Info.plist
    postBuildScripts:
      - name: Bundle Juno Agent Core
        script: |
          set -euo pipefail
          REPOSITORY_ROOT="$SRCROOT/../../.."
          npm --prefix "$REPOSITORY_ROOT/runner/agent-core" run bundle:mac
          RESOURCE_DIRECTORY="$TARGET_BUILD_DIR/$UNLOCALIZED_RESOURCES_FOLDER_PATH"
          mkdir -p "$RESOURCE_DIRECTORY"
          ditto \
            "$REPOSITORY_ROOT/runner/agent-core/dist/juno-agent-core.mjs" \
            "$RESOURCE_DIRECTORY/juno-agent-core.mjs"
        outputFiles:
          - $(TARGET_BUILD_DIR)/$(UNLOCALIZED_RESOURCES_FOLDER_PATH)/juno-agent-core.mjs
  JunoMacTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - path: Tests
    dependencies:
      - target: JunoMac
    configFiles:
      Debug: ../../Config/JunoMac-Debug.xcconfig
      Stable: ../../Config/JunoMac-Stable.xcconfig
      Next: ../../Config/JunoMac-Next.xcconfig
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
        PRODUCT_BUNDLE_IDENTIFIER: com.liammagnier.JunoMac.tests
  JunoMacUITests:
    type: bundle.ui-testing
    platform: macOS
    sources:
      - path: UITests
    dependencies:
      - target: JunoMac
    configFiles:
      Debug: ../../Config/JunoMac-Debug.xcconfig
      Stable: ../../Config/JunoMac-Stable.xcconfig
      Next: ../../Config/JunoMac-Next.xcconfig
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
        PRODUCT_BUNDLE_IDENTIFIER: com.liammagnier.JunoMac.uitests
        TEST_TARGET_NAME: JunoMac
schemes:
  JunoMac:
    build:
      targets:
        JunoMac: all
        JunoMacTests: [test]
        JunoMacUITests: [test]
    run:
      config: Debug
    test:
      config: Debug
      targets:
        - JunoMacTests
        - JunoMacUITests
    profile:
      config: Stable
    analyze:
      config: Debug
    archive:
      config: Stable
  JunoMac-Next:
    build:
      targets:
        JunoMac: all
    run:
      config: Next
    profile:
      config: Next
    analyze:
      config: Next
    archive:
      config: Next
