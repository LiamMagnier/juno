# Juno Native — Execution Roadmap

1. Audit and handoff baseline — complete (`1de5cda`).
2. Shared package and independent-project foundation — complete (`0fb7cc3`).
3. Production Keychain/browser auth composition, then durable SQLite/sync/search adapters — next.
4. macOS shell, chat, search, account flows, Juno Code, trust/permissions, Remote Host, and controlled Computer Use.
5. Adaptive iOS/iPadOS shell, chat, sidebar, search, account flows, Cloud Code and Remote.
6. Cross-surface convergence, offline/conflict/E2E, performance, accessibility, localization, approvals and push lifecycle.
7. Native CI, Release archives, signing/notarization/TestFlight, GitHub release, and production download metadata.

Each phase must end with focused tests, a reviewed diff, a coherent commit, and updated `STATUS.md`, `HANDOFF.md`, and `handoff.json` before the next phase begins.
