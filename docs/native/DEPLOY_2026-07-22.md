# Deploy — 2026-07-22, `a81078a`

## Result: healthy

| check | before | after |
|---|---|---|
| deployed SHA | `40dc9b4` | **`a81078a`** |
| homepage | 200 | 200 |
| `x-juno-contract-version` | 1.3.0 | 1.3.0 (unchanged) |
| `prisma migrate deploy` | — | **both migrations applied** |
| pm2 | 3 online | `juno-backend`, `juno-voice-relay`, `juno-scheduler` all online |

19 commits shipped as a clean fast-forward.

## Migrations applied

- `20260722200000_attachment_idempotency_key`
- `20260722210000_remote_code_sessions`

Both were checked for destructive risk before shipping: no `DROP`, `DELETE`,
`TRUNCATE`, `ALTER COLUMN` or `RENAME`, and no `ADD COLUMN ... NOT NULL` without
a default, which is the statement that fails on a non-empty table. Every `NOT
NULL` in the diff is inside a `CREATE TABLE` for a brand-new table.

## New routes verified live

| route | result | meaning |
|---|---|---|
| `POST /api/v1/attachments` | 401 | route exists, bearer required |
| `POST /api/code/devices/{id}/commands` | 401 | route exists, auth enforced |
| `POST /api/code/devices/{id}/sessions` | 405 | correct — that path is GET/PUT |

The attachments route answers in the v1 envelope, which is the whole reason it
exists separately from `/api/upload`:

```json
{"error":{"code":"unauthenticated","message":"A bearer token is required.",
"requestId":"req_...","retryable":false,"retryAfterMs":null}}
```

## Three-way parity

```
deployed server      a81078afd08322fa4fae3199ebc089bd40e5dd11
iPhone build         a81078afd0        contract 1.3.0
production contract  1.3.0
```

The iPhone was rebuilt from the deployed commit and reinstalled, so the phone and
the server now report the same commit from the Diagnostics screen. **The Mac app
has not been rebuilt at this SHA**, so the third leg of the comparison is not yet
closed.

## One hazard worth recording

The `main` *checkout* at `/Users/liammagnier/Desktop/workspace/juno` was being
edited by another session during this deploy — 40 dirty files with timestamps
minutes old, on an unrelated feature stream (model selector, thinking scale,
provider marks, native model manifest).

That checkout was **not touched**. The deploy pushed
`agent/juno-final-completion:main` by refspec, which fast-forwards the remote
without going near anyone's working tree. Their uncommitted work is intact on
disk, but `main` has moved 19 commits under them, and both they and this branch
edited `prisma/schema.prisma` and the Code-remote files — so that session will
hit a rebase conflict there and should expect it.
